#include <iostream>

/**
    This method calculates the Greatest Common Divisor (GCD) of a pair of numbers

    The GCD is calculated by
 
*/
int GCD(int a, int b){
    int out = 0;

    return out
}

int main() {
    // Test the function with some example values
    GCD(12, 24);
    GCD(1, 2);
    GCD(-3, 9);
    GCD(8, 13);
    GCD(256, 2);
    GCD(2, 256);
}
